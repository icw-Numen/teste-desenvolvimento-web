Tecnologias:
-Layout: Bootstrap
-Frontend: Express
-Backend: Node.js, MongoDB
-Deploy: Heroku

Como executar:
-Abra o terminal.
-Mude a diretoria para o mesmo em que este arquivo se encontra.
-Digite o comando "node app.js" se tiver Node.js instalado.

OU

-Abra no seu navegador de Internet o link que segue: 


Pastas:

-public:
-routes: Contém index.js, que define as routes que o site vai usar e quais páginas o site deve carregar.
-views:

Notas Adicionais:
-Procfile serve pra avisar a Heroku qual comando ela deve executar ao fazer deploy.
